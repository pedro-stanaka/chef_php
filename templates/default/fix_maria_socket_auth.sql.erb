

USE mysql;
UPDATE user SET password='<%= node['mariadb']['server_root_password'] %>',
    plugin = ''
WHERE User = 'root';

GRANT ALL PRIVILEGES ON *.* TO 'root'@'localhost' WITH GRANT OPTION;

FLUSH PRIVILEGES;
